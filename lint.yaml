name: Format and Auto-Fix

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  format:
    runs-on: ubuntu-latest
    steps:
      - name: 🚚 Checkout code
        uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref }} # Đảm bảo checkout đúng branch của PR

      - name: ⚙️ Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 8.0.x

      - name: 🧹 Dotnet Format
        # Bước này sẽ sửa lỗi format trực tiếp trên các file
        run: dotnet format

      - name: 💾 Commit and Push changes
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "style: 🧹 tự động format code .NET (GitHub Actions)"
          branch: ${{ github.head_ref }}
