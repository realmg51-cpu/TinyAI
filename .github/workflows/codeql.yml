name: "CodeQL Advanced"

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  analyze:
    name: Analyze (csharp)
    runs-on: ubuntu-latest
    permissions:
      security-events: write
      actions: read
      contents: read

    steps:
      - name: 🚚 Checkout repository
        uses: actions/checkout@v4

      - name: ⚙️ Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: 🔍 Initialize CodeQL
        uses: github/codeql-action/init@v3
        with:
          languages: csharp # Chỉ quét C#, bỏ qua các thứ khác 🎯

      - name: 🔨 Build Project
        # CodeQL cần build thành công mới phân tích được. 
        # Chúng ta trỏ thẳng vào file csproj trong thư mục asset.
        run: dotnet build TinyAI/asset/code/Main.csproj

      - name: 🚀 Perform CodeQL Analysis
        uses: github/codeql-action/analyze@v3
