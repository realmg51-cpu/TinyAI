name: 🧹 Lint & Format

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

# Thiết lập quyền hạn ở mức toàn cục để bảo mật tối đa
permissions:
  contents: read

jobs:
  lint:
    name: 🧹 Lint Code
    runs-on: ubuntu-latest
    
    steps:
      - name: 🚚 Checkout code
        uses: actions/checkout@v4

      - name: ⚙️ Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: ✨ Check Format
        # Lệnh này kiểm tra xem code có đúng chuẩn không.
        # --verify-no-changes: Nếu code chưa format thì sẽ báo lỗi ngay (fail build).
        run: dotnet format TinyAI/asset/code/main.csproj --verify-no-changes --verbosity diagnostic
