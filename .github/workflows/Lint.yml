name: 🧹 Lint & Format

on:
  push:
    branches: [ "main" ]
    paths:
      - 'asset/code/*.cs'
      - 'asset/code/*.csproj'
  pull_request:
    branches: [ "main" ]

permissions:
  contents: read

jobs:
  lint:
    name: 🧹 Lint Code
    runs-on: ubuntu-latest
    
    steps:
      - name: 🚚 Checkout code
        uses: actions/checkout@v4

      - name: ⚙️ Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: ✨ Check Format
        # Trỏ thẳng vào main.csproj để không bị lẫn với file trong thư mục Backup
        run: dotnet format asset/code/main.csproj --verify-no-changes --verbosity diagnostic
