name: 🧹 Lint & Format

on:
  push:
    branches: [ "main" ]
    # Chỉ chạy khi các file sau thay đổi:
    paths:
      - '**.cs'
      - '**.csproj'
  pull_request:
    branches: [ "main" ]
    paths:
      - '**.cs'
      - '**.csproj'

permissions:
  contents: read

jobs:
  lint:
    name: 🧹 Lint Code
    runs-on: ubuntu-latest
    
    steps:
      - name: 🚚 Checkout code
        uses: actions/checkout@v4

      - name: ⚙️ Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: ✨ Check Format
        # Con Bot bây giờ chỉ soi đúng file project của bạn
        run: dotnet format TinyAI/asset/code/main.csproj --verify-no-changes --verbosity diagnostic
