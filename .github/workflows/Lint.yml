name: 🧹 Lint & Format

on:
  push:
    branches: [ "main" ]
    paths:
      - '**.cs'
      - '**.csproj'
  pull_request:
    branches: [ "main" ]
    paths:
      - '**.cs'
      - '**.csproj'

permissions:
  contents: read

jobs:
  lint:
    name: 🧹 Lint Code
    runs-on: ubuntu-latest
    
    steps:
      - name: 🚚 Checkout code
        uses: actions/checkout@v4

      - name: ⚙️ Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: ✨ Check Format
        # Tự động tìm file .csproj để format mà không cần chỉ định tên chính xác
        run: dotnet format asset/code/*.csproj --verify-no-changes --verbosity diagnostic
