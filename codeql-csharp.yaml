name: "CodeQL Daily & Push"

on:
  push:
    branches: [main] # Chạy mỗi khi bạn push lên nhánh main
  pull_request:
    branches: [main] # Chạy khi tạo PR vào main
  schedule:
    - cron: '0 17 * * *' # Chạy hàng ngày lúc 00:00 giờ VN (17:00 UTC) ⏰

jobs:
  analyze:
    name: Analyze
    runs-on: ubuntu-latest
    permissions:
      security-events: write
      actions: read
      contents: read

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4 # Đã lên v4 cho xịn 🚀

      - name: Initialize CodeQL
        uses: github/codeql-action/init@v3 # Lên v3 luôn
        with:
          languages: 'csharp'

      - name: Autobuild
        uses: github/codeql-action/autobuild@v3

      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@v3
